# logs_filter

## 目的

这个项目是为了制作一个工具可以快速从`NavigationService.g3log.20251124-200157.37951.log`这类日志中提取出符合特定格式要求的记录并保存到对应的`can_service.g3log.20251124-200157.37951.log`文件中，文件名中的`.g3log.20251124-200157.37951.log`保持与输入文件一样，只需要从`NavigationService`另存为`can_service`。

## 匹配公式

^\d{2}:\d{2}:\d{2} \d{3}\(\d{4}\)[A-F]/[a-zA-Z_]\w*:\s*(?:|.*<===|.*===>|.*=|.*:|[a-zA-Z_]\w*(?: [a-zA-Z_]\w*)*)(?:\s*[0-9a-fA-F]{1,2}){1}(?:\s[0-9a-fA-F]{1,2}){7}(?:\s*|\s*,.*|\s*\[(?:(?:0x)?[0-9a-fA-F]{1,2}(?:\s*(?:0x)?[0-9a-fA-F]{1,2})*)\])$

## 编译

```bash
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true
```

## AI提示词

```
我需要完成以下功能：
1.处理同级目录下所有名称匹配这个正则表达式的文件：^NavigationService\.g3log\.*\.log$
2.处理每个文件时，对所有行进行正则匹配：^\d{2}:\d{2}:\d{2} \d{3}\(\d{4}\)[A-F]/[a-zA-Z_]\w*:\s*(?:|.*<===|.*===>|.*=|.*:|[a-zA-Z_]\w*(?: [a-zA-Z_]\w*)*)(?:\s*[0-9a-fA-F]{1,2}){1}(?:\s[0-9a-fA-F]{1,2}){7}(?:\s*|\s*,.*|\s*\[(?:(?:0x)?[0-9a-fA-F]{1,2}(?:\s*(?:0x)?[0-9a-fA-F]{1,2})*)\])$
3.满足第二点的行，保留前面的：^\d{2}:\d{2}:\d{2} \d{3}\(\d{4}\)[A-F]/[a-zA-Z_]\w*:
4.然后将本行后面的符合这个正则表达式的内容保留：(?:\s*[0-9a-fA-F]{1,2}){1}(?:\s[0-9a-fA-F]{1,2}){7}
5.将第四点保留下来的内容进行下面类似的替换：从11 22 33 44 55 66 77 88替换为用中括号括起来的内容：[0x11 0x22 0x33 0x44 0x55 0x66 0x77 0x88]
6.然后将本行的其余内容全部删除，只保留第三点保留下来的和第五点替换后的内容。
7.新内容输出到新文件中，格式如下：从NavigationService.g3log.20251124-200157.37951.log提取到can_service.g3log.20251124-200157.37951.log中，以此类推。
请基于当前的C#工程进行。
```
